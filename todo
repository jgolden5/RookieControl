0.0 Pre-coding/setup/documentation/questions/misc ✅
  0.1 began design-decisions doc ✅
  0.2 added readme to backend with maven cli launch instructions ✅
  0.3 created very simple api.md file with basic GET for documents by code ✅
  0.4 added todo ✅
  0.5 added run configuration for main and tests in IntelliJ ✅
  0.6 questions to ask senior dev
    0.6.1 Should I use Digital Ocean or local server for Rookie Control--if it's the latter, when can I set this up?
    0.6.2 How can I identify what a docker image involves just by looking at the image on dockerhub?
    0.6.3 Is it ok to put testing and running in the same docker container, or is it a best practice to put tests into their own containers?
1.0 Proof of concept API setup (no database) ✅
  1.1 scaffolding for API POC ✅
    1.1.1 basic healthcheck api endpoint passes (super basic Spring Boot API backend working already) ✅
    1.1.2 added DocumentDto (response content server -> client) ✅
    1.1.3 added DocumentService (business logic) ✅
    1.1.4 added DocumentController (HTTP layer) ✅
  1.2 tests and exceptions for API POC
    1.2.1 added DocumentController test, which tests DTO, service, and controller ✅
    1.2.2 added DocumentNotFoundException, throw it from the service, then validate input in the controller ✅
    1.2.3 added GlobalExceptionHandler ✅
2.0 Proof of concept database integration with API
  2.1 go through spring.io tutorial for data access with MySQL = https://spring.io/guides/gs/accessing-data-mysql ✅
    2.1.0 (refer to Gemini chat asking about the "Setting up the MySQL Database" section of the Spring.io guide) = https://gemini.google.com/gem/a0d79bfb814f/c600afcc86f9f071 ✅
    2.1.1 update pom.xml file to include spring data JPA, MySQL Driver, and Docker Compose support (refer to gemini and spring.io links above) ✅
    2.1.2 create the compose.yaml file ✅
    2.1.3 verify that the docker compose is working (ask Gemini if there's a way to make a test for this) ✅
  2.2 build a persistent database using JPA's and Entities
    2.2.1 turn DTO into Entity ✅
    2.2.2 create DocumentRepository interface, which extends JpaRepository<Document, Long>
    2.2.3 refactor DocumentService to find documentRepository by code using the findDocumentByCode function added to DocumentRepository interface
    2.2.4 configure automated table configuration by adding src/main/resources/application.properties with configs for said automation
    2.2.5 test these files manually with src/main/resources/import.sql (this file gets run automatically when the app starts, so the name is important)
    2.2.6 add an integration test and/or unit tests to make sure at least the most important aspects of the persistent database's functionality is working
  2.3 integrate said database with API's, and make sure I have accurate tests set up to reflect this
3.0 Proof of concept front end (integrated with database, APIs, and some minimal business logic) [POC phase complete!]
